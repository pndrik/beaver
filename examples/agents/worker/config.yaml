---
metadata:
  name: worker
  display_name: Bert
  description: A worker agent that performs tasks assigned by the conductor.
inference:
  model: gemini-3-flash
tools:
  permissions:
    - name: file
      scopes:
        - AI/
