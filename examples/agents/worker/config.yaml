---
metadata:
  name: worker
  display_name: Bert
  description: A worker agent that performs tasks assigned by the conductor.
  model:
    name: gemini-3-flash
permissions:
  tools:
    - name: file
      scopes:
        - AI/
