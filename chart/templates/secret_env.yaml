apiVersion: v1
kind: Secret
metadata:
  name: {{ include "chart.name" . }}-env
  labels: {{ include "chart.labels" . | nindent 4 }}
type: Opaque
stringData:
{{- range $key, $value := .Values.env }}
  {{ $key }}: {{ tpl $value $ | quote }}
{{- end }}
